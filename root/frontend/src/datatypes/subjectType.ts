export type Subject = {
  subjectId: number;
  subjectCode: string;
  subjectName: string;
  description: string;
  creditHours: number;
};

export type StudentSubjectData = {
  subjectId: number;
  subjectCode: string;
  subjectName: string;
  creditHours: number;
}

